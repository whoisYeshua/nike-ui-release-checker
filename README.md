### HTTPS local

1. Start local server

   ```bash
   npm run dev:mobile
   ```

2. Add certificate to trusted

On macOS: Keychain Access -> System Keychains -> System -> Certificates (drag and drop `_cert.pem` here)
